let express,cors;_b9c‍.w("express",[["default",["express"],function(v){express=v}]]);_b9c‍.w("cors",[["default",["cors"],function(v){cors=v}]]);


const morgarn = require("morgan");
require("dotenv").config();

//create express app
const app = express();

//apply middlewares
app.use(cors());
app.use(express.json());
app.use(morgarn("dev"));
app.use((req,res,next))

//route
app.get('/', (req, res) => {
    res.send('server end');
});

// port
const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`server is ruuning on port ${port}`));